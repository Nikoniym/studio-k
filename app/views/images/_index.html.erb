<% @images.each do |image| %>
    <div class="img-thumbnail">
      <%= image_tag image.avatar.url(:thumb), alt: image.avatar.url(:thumb) %>
      <%= link_to 'Destroy', album_image_path(image.album,image),
                  method: :delete %>
    </div>
<% end %>