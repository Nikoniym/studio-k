<% if @answer %>
  $(".write_user tbody").append("<%= escape_javascript(render('add_user')) %>");
  $(".ready_user tr[data-id='<%=@user.id %>']").replaceWith("<%= escape_javascript(render('remove_user')) %>");
  $('.table_name .place_current').text('<%= @lesson.place_current %>');
  $('.users_list .place_current').text( "Количество свободных мест: <%= @lesson.place_current %>");
  $(".paid_subscription").html("<%= escape_javascript(render 'lessons/paid') %>");
<% end %>