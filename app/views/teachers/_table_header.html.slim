|#{@table.active if @current_user.has_role? :admin} #{@table.day_week} #{ l @table.date, format: :standard} #{@table.time_spending} #{@table.training_name}
/span.place_current = @table.place_current
br
- if @table.active
  = link_to 'Отменить', destroy_lesson_teacher_path(@table) if @current_user.has_role? :admin
- else
  = link_to 'Активировать', active_lesson_teacher_path(@table)  if @current_user.has_role? :admin